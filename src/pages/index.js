import React, { useState } from 'react';
import Switch from 'react-switch';
import Head from 'next/head';
import Image from 'next/image';
import { Navbar, Main, About, Tech, Projects } from '../../components';
import { BsFillSunFill, BsFillMoonFill } from 'react-icons/bs';

export default function Home() {
  const [darkMode, setDarkMode] = useState(false);

  const DarkButton = () => (
    <Switch
      onChange={ () => setDarkMode(!darkMode) }
      checked={ darkMode }
      uncheckedIcon={<BsFillSunFill className='m-auto h-7 text-light-main-bg' />}
      checkedIcon={<BsFillMoonFill className='m-auto h-7 text-light-main-bg' />}
      onColor='#5651E5'
      offColor='#709DFF'
    />
  )

  return (
   <html className={ darkMode ? 'dark' : '' }>
    <Head>
      <title>Donna Wong | Full Stack Software Engineer</title>
      <meta name='description' content='generated by create next app' />
      <link rel='icon' href='/assets/logo.png' />
    </ Head>
    <body>
      <Navbar DarkButton={ DarkButton } />
      <Main />
      <About />
      <Tech />
      <Projects />
    </body>
   </html>
  )
}
